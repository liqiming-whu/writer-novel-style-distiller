---
name: writer-novel-style-distiller
description: 小说文风蒸馏、文笔量化与条件式幽默感审计、主角魅力及完结收束分析工具。用户提供小说正文或 TXT／Markdown／DOCX／EPUB，要求文风蒸馏、可复用档案／Skill、主角魅力分析时使用；用户明确说明作品已完结时，额外强制分析主线结局、尾声及已提供番外，提取可迁移的结局设计机制。普通续写或仅问写作建议时不自动触发。
metadata:
  hermes:
    tags: [novel, style, distillation, protagonist, charm, skill-builder]
    requires_toolsets: [terminal, file]
    related_skills: [writer-task-dispatcher, writer-writing-style-extractor, writer-novel-writing-protocol, writer-persistence-write-gate]
---

# 小说文风蒸馏器

## 定位

本 Skill 把用户有权提供的小说样本转化为可复用的“风格操作系统”，同时解释主角为何让读者觉得可爱、心疼、敬佩或着迷。

目标不是摘抄金句，也不是把题材词、人物口癖和剧情设定误当文风，而是完成四件事：

1. 从全书或可靠样本中提取稳定、可执行、可验证的文风规律。
2. 把“主角很可爱／很迷人”拆成文本机制、人物机制和关系机制，并指出证据与失效风险。
3. 把结果写入自包含参考文档，后续无需重新读取原小说也能复用。
4. 可按用户要求生成、校验、打包并安装独立的样本文风 Skill。
5. 当用户明确说明小说已经完结时，强制分析主线结局、尾声与已提供番外，蒸馏可迁移的长篇收束机制，为后续结局设计提供证据化参考。
6. 维护经过校验的内置可复用档案库；用户点名档案或要求从已有档案中选择时，直接加载档案，不重新读取原小说。
7. 对每份完整档案执行文笔审计，以语言控制、对白塑造、画面氛围、情绪感染、信息组织、关系亲密感、收笔与留白及综合文笔八项给出十分制量化评分、证据摘要和扣分点。
8. 在且仅在作品存在跨位置、跨场景、具有明确叙事功能的幽默系统时，追加幽默感审计；没有明显幽默感的作品省略该章节，不给零分，不强行寻找笑点。

“忠实还原”指尽量还原叙事结构、距离、节奏、对白机制、情绪推进和修辞选择，不复制原作剧情、长段原文、专属设定、标志性句子或角色身份。

---

## 路由与优先级

### 应当触发

- 用户上传或粘贴小说，并要求分析、蒸馏、保存或生成文风 Skill。
- 用户要求解释主角为何可爱、迷人、令人着迷，且需要结合小说文本。
- 用户要求更新已有文风档案、补充新章节样本、比较前后期文风变化。
- 用户明确说明样本小说已经完结；此时结局、尾声以及已提供番外属于强制分析范围，不得只做早中晚抽样后跳过。
- 用户要求把已蒸馏文风打包、安装或复用到后续创作。

### 不应抢占

- 普通新写、续写、扩写、改写：交给正文协议。
- 用户只问某个写作概念：直接回答，不启动重型蒸馏。
- 用户只想得到简短印象：使用轻量文风分析，不创建资产。

### 与 `writer-writing-style-extractor` 的分工

- 简单收集、展示 Q10A 文风摘要：可用原提取 Skill。
- 全小说、定量分析、魅力机制、证据账本、复用档案或子 Skill：使用本 Skill。

### 内置可复用档案

先读取 [档案目录](profiles/CATALOG.md)，再按任务条件加载目标档案。当前内置档案包括：

- `001-youfei-priest`：《有匪》样本衍生文风，含文笔审计、完整文风操作系统、执行卡、谢允魅力机制、完结收束与番外设计、证据账本和公开来源清单。
- `002-hanshanji-kanchangtingwan`：《寒山纪》样本衍生文风，含文笔审计、完整文风操作系统、执行卡、洛元秋魅力机制、第225章结局与第226章番外设计、证据账本和公开来源清单。
- `003-longzu-jiangnan`：《龙族1—4》样本衍生文风，含文笔与幽默审计、完整文风操作系统、执行卡、路明非代入机制、楚子航与绘梨衣角色魅力、四个卷级尾声、证据账本和公开来源清单。
- `004-sanqianji-xitu`：《三千纪》样本衍生文风，含文笔与幽默审计、完整文风操作系统、执行卡、月儿兰／格林身份连续性与长期依恋机制、未完结边界、现存插篇与外篇功能、证据账本和来源清单。
- `005-yifutelona-jiuri-juzer`：《伊芙特罗娜的旧日见闻》样本衍生文风，含文笔与幽默审计、完整文风操作系统、执行卡、伊芙身份连续性与读者依恋机制、第366章关系及接受争议的证据分层、未完结边界、证据账本和来源清单。

加载纪律：

1. 普通风格创作／改写：读取该档案的 `style-profile.md` 与 `application-card.md`。
2. 设计主角魅力：再读取 `protagonist-charm.md`。
3. 设计终卷、结局、尾声或番外：再读取 `ending-design.md`。
4. 解释结论或审计漂移：读取 `evidence-ledger.md` 与 `source-manifest.json`。
5. 不把档案自动升级为全局用户偏好；只有用户点名使用、当前项目明确启用或任务要求比较档案时才加载。
6. 内置档案只含抽象分析与最小证据定位，不含原始小说、清洗语料或连续长段原文。

---

## 执行边界

1. 先读取当前工作区 `AGENTS.md`，确认项目类型、允许写入范围和活动项目。
2. 文件处理遵循“读取最新内容 → 最小写入 → 回读验证”。未得到明确成功回执，不得声称已保存、已打包或已安装。
3. 只分析用户提供、用户授权访问或合法公开获取的文本。不要主动下载盗版小说。
4. 原始小说与清洗语料只放在临时分析区或用户指定的私有目录，不进入可分发 Skill 包。
5. 参考文档不保存连续长段原文。证据优先记录章节、段落哈希、场景类型和释义；确需短引时保持最小必要长度。
6. 对在世作者或可识别作品，不以“冒充作者本人”为目标。生成资产命名为“样本衍生文风”，描述可执行特征，不宣称官方、原作续篇或作者本人创作。
7. 题材惯例、角色口癖、专属名词、单场景偶发现象必须与稳定文风分开。
8. 所有精确比例都是样本观测值或宽容区间，不是创作配额。禁止为了命中数字把文章写成统计模型。
9. 如果样本不足、章节识别失败或前后期差异过大，诚实降级为“暂定档案”，不得假装高保真。
10. “已完结”只接受用户明确声明或可靠出版／目录信息，不得仅因文件末尾出现“终章”便擅自认定；一旦成立，必须区分主线结局、尾声／后日谈、番外／外传与作者后记，不能把作者后记当作正史，也不能用番外代替对主线结局的分析。
11. 用户称作品已完结但输入未包含主线结尾，或已确认存在番外却未提供时，必须标记缺失并请求补充或降级交付；若番外是否存在无法确认，只记录“无法确认”，不得凭作品常识补写、推测或伪造未提供内容。

---

## 任务模式

收到请求后，选择且只选择当前主模式：

- **COLLECT**：只接收和登记样本，不分析。适用于用户明确说还要继续发送。
- **DISTILL**：样本已齐或用户要求立即分析。完成清洗、统计、精读、魅力分析和档案写入。
- **UPDATE**：增量读取新样本，比较旧档案，只修订被新证据影响的条目。
- **BUILD**：已有蒸馏结果，生成独立风格 Skill。
- **AUDIT**：检查已有文风档案／风格 Skill 的证据、漂移和可执行性。
- **INSTALL**：校验后安装指定 Skill；不在校验失败时强行覆盖。

信息足够时直接执行。仅在缺少小说文本或保存目标完全不明确时，提出一个最小问题。

---

## 标准流程

### Phase 0：建立任务与输出位置

1. 确认输入文件、粘贴文本、小说标题（可未知）、主角识别方式和用户是否要生成独立 Skill。
2. 记录作品状态为 `unknown`、`ongoing` 或 `complete`，并记录认定依据。只有用户明说“已完结”或来源目录提供可靠完结信息时才设为 `complete`；不得靠模型常识猜测。
3. 若状态为 `complete`，盘点输入是否包含主线终局、尾声／后日谈、番外／外传和作者后记，并把“存在但未提供”与“确认不存在”分开记录。
4. 确认唯一活动项目。不可把两个小说项目的样本混在同一档案中。
5. 确定输出目录：
   - 用户指定路径优先。
   - 工作区允许新增目录时，默认使用 `<项目根>/style-profiles/<slug>/`。
   - 工作区限制长期文件类型时，写入用户明确允许的候选文风文件，或把独立资产输出到 Operit 私有文风目录；不得绕过 `AGENTS.md`。
6. 创建分析区与交付区，二者分离：

```text
<job-root>/
├── work/                         # 临时分析数据，不打包
│   ├── corpus.normalized.txt
│   ├── chapters.jsonl
│   ├── sample-plan.json
│   └── metrics.json
└── deliverable/
    └── <style-slug>/
        ├── SKILL.md              # 生成独立Skill时存在
        ├── FIDELITY.md
        └── references/
            ├── style-profile.md
            ├── application-card.md
            ├── protagonist-charm.md
            ├── ending-design.md
            ├── evidence-ledger.md
            └── source-manifest.json
```

7. 若用户只要参考档案，不生成独立 Skill，仍保留 `references/` 内六份交付文档；未明确完结时，`ending-design.md` 只记录“未触发／证据不足”，不得假装已有结局规律。

### Phase 1：完整读取、清洗与分章

按需读取 [source-handling.md](references/source-handling.md)。

1. TXT／Markdown／HTML／DOCX／EPUB 优先运行：

```bash
python3 <skill>/scripts/ingest_novel.py INPUT... --output <job-root>/work
```

用户已明确说明作品完结时，改用：

```bash
python3 <skill>/scripts/ingest_novel.py INPUT... --output <job-root>/work \
  --completion-status complete --completion-basis user_explicit
```

2. 脚本必须输出编码、SHA-256、字符数、章节数、完结状态、终局／尾声／番外候选单元和清洗警告。
3. 文件很长也必须覆盖全文。不能只读文件首块、只凭文件名推断、也不能默认只取前二十章。
4. 无章节标记时按稳定窗口切段，并把“伪章节”明确标注为窗口，不得称为原书章节。
5. 清洗后随机回读开头、中段、后段和结尾，确认没有把前言、正文或作者叙述误删。
6. 状态为 `complete` 时，必须回读主线终局前的因果链、最终章、尾声／后日谈以及每篇已提供番外；作者后记只用于理解创作说明，不得混入正史结局证据。章节标题无法可靠区分时，人工核对目录和文本边界。

### Phase 2：双轨取样

本 Skill 同时使用：

- **全量统计轨**：对全部清洗正文计算句段、对白、标点、视角提示、连接词、修辞标记、章节开头与结尾等分布。
- **分层精读轨**：覆盖开篇、前段、中段、后段、结尾，以及日常、冲突、抒情、战斗／行动、信息揭示和关系场景。

运行：

```bash
python3 <skill>/scripts/measure_style.py --workdir <job-root>/work
```

精读样本选择规则详见 [extraction-framework.md](references/extraction-framework.md)。样本较长时至少覆盖八个章节／窗口；全书很长时优先分层取样，而不是顺序读完开头后停止。

若状态为 `complete`，另开“终局全覆盖轨”：主线终局前的关键蓄势段、最终选择、主线结局、尾声和全部已提供番外不受普通抽样上限挤占。番外很多时可以分组归纳功能，但必须逐篇确认其时间位置、视角、正史状态与收束作用。

### Phase 3：证据化文风蒸馏

逐项执行 [extraction-framework.md](references/extraction-framework.md)，形成“稳定核心—可变层—角色层—题材层”的分层结论。

每条重要结论必须记录：

- 结论内容。
- 证据位置，至少精确到章节／窗口和场景类型。
- 全量指标或精读观察。
- 复现范围：出现于多少个独立位置、多少类场景、早中晚哪些阶段。
- 置信度：高／中／低。
- 反证或例外。
- 转化为写作动作后的可执行指令。
- 过量使用时的漫画化风险。

不得只写“细腻、优美、节奏好、人物鲜活”。任何形容词都要落到句法、段落、动作、对白、视角、信息或情绪机制上。

### Phase 3.5：文笔审计与量化评分

常规文风证据稳定后，按 [extraction-framework.md](references/extraction-framework.md) 的维度17执行八项文笔审计：语言控制、对白塑造、画面氛围、情绪感染、信息组织、关系亲密感、收笔与留白、综合文笔。

1. 每项使用十分制并保留一位小数。
2. 每项必须同时写依据摘要和主要扣分点，禁止只给裸分。
3. 综合分可以按作品类型重心审慎加权，不要求机械平均，但必须说明总体判断。
4. 评分是当前样本在统一审计口径下的启发式定位，不是客观文学排名或后续创作配额。
5. 将本节写入 `style-profile.md`，且必须紧接“一句话核心文风”，位于“稳定文风核心”之前。
6. 源文件排版、抓取或编码噪声影响判断时，先修正语义副本或公开声明限制，不把来源错误算成作者文笔。

### Phase 3.6：幽默感审计（条件触发）

按 [extraction-framework.md](references/extraction-framework.md) 的维度18先做触发判断。幽默至少跨三个独立位置和两类场景稳定复现，并且参与人物、关系、节奏、情绪或读者位置时，才在 `style-profile.md` 中加入“幽默感审计”。

触发后：

1. 章节必须紧接“文笔审计与量化评分”，位于“稳定文风核心”之前。
2. 完整分析主要机制、节奏与场景分布、角色分工、情绪关系协同、读者位置、伦理边界、可迁移方法与失效风险。
3. 使用机制多样性、人物绑定度、节奏与时机、声音区分度、严肃场景兼容、克制与反漫画化、情绪转化能力、综合幽默完成度八项评分。
4. 评分衡量幽默系统完成度，不衡量笑点数量；黑色幽默、冷幽默和低密度幽默可以获得高分。

未触发时：

1. 完全省略该章节，将模板中的 `{{OPTIONAL_HUMOR_AUDIT_SECTION}}` 替换为空文本。
2. 不给零分、低分或“无幽默”占位结论；严肃作品不因缺少幽默被扣分。
3. 不把轻松、可爱、荒诞或讽刺强行认定为幽默。

### Phase 4：主角可爱与着迷机制

读取 [protagonist-charm-framework.md](references/protagonist-charm-framework.md)。

先验证用户指定角色确为主要视角／核心主角；若多主角，分别分析，不把群像魅力强行集中到一人。

分析必须回答：

1. 读者首先被什么吸引：外在呈现、叙述声音、能力、欲望、处境或关系。
2. “可爱”来自何种组合，而非单一标签。
3. 脆弱是否保留行动能力，强大是否附带代价与克制。
4. 角色如何关心别人，别人为何愿意反过来关心她／他。
5. 公共形象与私人状态的反差如何被叙述距离放大。
6. 小习惯、窘迫、嘴硬、失败、道歉和修复如何制造亲近感。
7. 哪些魅力来自人物本身，哪些来自叙述者偏爱或配角集体认证。
8. 读者着迷的持续机制是什么：期待、保护欲、敬佩、共谋感、欲望张力还是成长承诺。
9. 哪些写法会让可爱变成幼化、完美化、媚读者或全员捧场。
10. 如何复用机制而不复制该主角。

结论写入 `protagonist-charm.md`。每个“魅力因子”使用“文本证据 → 读者心理机制 → 可复用写法 → 过量风险”结构。

### Phase 4.5：完结作品的结局与番外设计蒸馏

仅当作品被可靠标记为 `complete` 时强制执行，并写入 `ending-design.md`。必须回答：

1. 开篇与中段承诺了哪些人物、关系、世界、悬念和主题问题；主线结局分别如何兑现、转化或有意留白。
2. 最终高潮由什么长期因果触发，主角的最终选择是否主动、付出何种代价，并如何完成或反转人物弧。
3. 外部冲突、内部欲望、核心关系与世界秩序分别收束到什么状态；哪些线索在终局回收，哪些开放项属于有效余味，哪些可能只是遗漏。
4. 高潮之后保留了多少降压与余波；情绪落点属于凯旋、苦甜、悲剧、开放、循环、归家、远行或其他结构，文本如何让读者从高压过渡到接受。
5. 最终场景选择了谁的视角、何种叙事距离、时间跨度、意象、对白或可见动作；末句为何能停止，而非只因故事材料耗尽。
6. 尾声／后日谈承担的是结果确认、未来证明、关系落地、主题回声还是世界余波；它是否补足主结局，而非重复主结局。
7. 每篇番外的时间位置、视角人物、正史状态和功能：日常补偿、配角闭环、关系兑现、未解线索修复、未来切片、喜剧降压、前史补充或实验性／非正史内容。
8. 番外是否改变对主结局的理解；若删除番外，主线结局是否仍然自足。不能用番外替主线偿还本应完成的核心承诺，除非档案明确把这种延迟兑现识别为有代价的样本策略。
9. 从上述证据中提取“结局设计动作”，分别说明适用前提、可迁移结构、失败模式和不可复制的原作表层。

结局分析不是复述剧情。输出应形成“承诺 → 最终选择／代价 → 多层闭环 → 情绪降落 → 尾声／番外余波”的设计模型，并在证据账本中为关键判断登记终局位置。未提供番外时只写“未提供／确认不存在／无法确认”中的真实状态。

### Phase 5：生成可复用参考文档

按 [reusable-profile-schema.md](references/reusable-profile-schema.md) 和 `templates/` 生成：

- `style-profile.md`：完整文风操作系统；“一句话核心文风”后必须紧接文笔审计与八项量化评分；存在稳定幽默系统时再追加幽默感审计，否则省略。
- `application-card.md`：后续创作时可快速加载的精简执行卡。
- `protagonist-charm.md`：主角魅力机制，不混入全局文风。
- `ending-design.md`：完结作品的承诺回收、最终选择、闭环层级、情绪降落、尾声与番外功能；未明确完结时只记录未触发状态。
- `evidence-ledger.md`：结论、证据、置信度、例外和来源定位。
- `source-manifest.json`：文件哈希、编码、范围、统计日期和样本覆盖。

写完逐份回读。任何文件仍含占位符、空章节或互相冲突的“当前值”，都视为未完成。

### Phase 6：构建独立样本文风 Skill

用户要求后续直接复用或生成 Skill 时：

1. 复制 [generated-style-skill.md](templates/generated-style-skill.md) 为交付目录的 `SKILL.md`。
2. 使用中性、可辨识且合法的 hyphen-case 名称，例如 `writer-sample-style-<slug>`。
3. SKILL.md 只负责触发、加载顺序、任务边界和执行纪律；详细风格全部放在 `references/`，避免主文件过长。
4. 写作时先加载 `style-profile.md` 与 `application-card.md`；涉及人物魅力设计时再加载 `protagonist-charm.md`；涉及终卷、最终高潮、结局、尾声或番外设计时再加载 `ending-design.md`。
5. 生成 Skill 必须包含反漂移规则：
   - 当前用户指令与项目事实优先。
   - 不复制原作人物、剧情、专属名词和标志句。
   - 不把所有高频特征同时拉满。
   - 低置信度特征不得升级为硬规则。
   - 不以统计指标牺牲自然表达。
6. 不把 `work/`、原始正文、清洗语料或长摘录打入 Skill 包。

### Phase 7：保真验证

读取 [fidelity-protocol.md](references/fidelity-protocol.md)。

至少完成：

1. **静态校验**：目录、frontmatter、引用路径、占位符、证据账本、诚实边界。
2. **指标回放**：生成测试文本后与样本的宽容区间比较；只把语域穿帮、视角违规等判为硬失败，密度偏差多为警告。
3. **场景覆盖测试**：日常对白、情绪转折、行动／冲突、信息说明至少取三类测试，防止只会一种场景。
4. **反漫画化测试**：检查笑点、短句、口癖、比喻、金句是否被浓缩得比原作更密。
5. **独立评审**：条件允许时由未参与蒸馏的评审模型盲读；不允许生成者自评自证。没有独立评审能力时，明确标记“静态校验通过，盲测未执行”。
6. **魅力保真测试**：验证生成的新主角是否复用了依恋机制，而非复制原主角身份、经历和口癖。
7. **结局设计迁移测试**：来源明确完结时，以全新人物与冲突设计“承诺回收—最终选择—余波—停止点”，核对其迁移的是收束机制而非原作结局事件；同时检查尾声和番外是否各有必要功能。

运行静态检查：

```bash
python3 <skill>/scripts/validate_profile.py <deliverable>/<style-slug>
```

打包：

```bash
python3 <skill>/scripts/package_skill.py <deliverable>/<style-slug> --output <dist-dir>
```

### Phase 8：安装与启用

只有用户要求安装时执行。

1. 安装前再次运行 `validate_profile.py`。
2. 使用：

```bash
python3 <skill>/scripts/install_skill.py <style-skill-dir> --skills-root <Operit-skills-root>
```

3. 已存在同名 Skill 时默认停止；用户明确允许替换才加 `--replace`，并建立备份。
4. 安装后重新读取安装目录中的 `SKILL.md`，再通过宿主的 Skill 加载机制实际激活一次。
5. 只有加载成功才能回复“已启用”。目录复制成功但加载失败时，只能说“已安装，尚未启用”。

---

## 置信度与样本门槛

详细规则见 [evidence-confidence.md](references/evidence-confidence.md)。速查如下：

- **高置信**：跨至少三个独立位置、两类场景，并有全量指标或明确反复模式支持。
- **中置信**：跨至少两个位置，或只有精读证据但机制清楚。
- **低置信**：单场景、单人物口癖、章节阶段特有现象、自动脚本弱推断。
- **不得下结论**：证据互相矛盾且无法解释，或样本不足以区分题材惯例与作者习惯。

少于约八千有效中文字或不足两个完整场景时，不生成“高保真”标签；可以交付暂定档案和缺口清单。

---

## 更新已有档案

1. 读取旧 `source-manifest.json` 与新文件哈希，避免重复分析。
2. 对新样本运行相同指标，比较早期／旧样本与新增部分的漂移。
3. 稳定核心只有在新证据足够强时才修改；角色口癖和阶段语域优先进入可变层。
4. 更新 `evidence-ledger.md`，保留被否决条目的历史原因，不在主档案同时保留互斥当前值。
5. 重新跑静态校验与相关场景测试。

---

## 输出纪律

- 收集阶段只回报已收录范围、文件校验和下一步，不提前分析。
- 蒸馏完成时总结：分析范围、核心文风、文笔审计总分与主要长短板；幽默感审计实际触发时再总结其核心机制与完成度，未触发时不强行添加；随后报告主角魅力机制、保存路径、保真等级和未决缺口。来源明确完结时，另报主线结局、尾声及番外的实际覆盖状态和结局设计模型。
- 不向用户倾倒内部脚本日志、JSON 全文或逐步思维过程。
- 用户只要求分析时，不擅自续写小说。
- 用户要求生成正文时，生成的风格 Skill 只输出目标正文，不在正文前后添加分析说明。

---

## 失败与降级

- 文件无法解码：报告尝试过的编码，请用户提供 UTF-8 或允许转码。
- 章节识别不稳：切换窗口模式，并降低章首／章末结论置信度。
- 样本太长导致单轮无法精读：全量统计照常完成，精读按分层计划分轮落盘，不从头重来。
- 主角身份不明确：只问“要分析哪位主角”，不猜。
- 脚本结果与人工精读冲突：人工证据优先，脚本作为提示；在账本记录冲突。
- 工作区禁止目标写入：停止持久化，给出允许的候选路径，不伪造成功。
- 盲测不可用：交付静态和指标校验结果，明确未做独立盲测。

---

## 上游经验与原创边界

本 Skill 的设计吸收了多种开源项目中的通用经验：多维文风拆解、全流程状态化、证据分级、忠实度盲测、反漫画化、模板构建和可分发 Skill 结构。具体来源、版本、许可证和未直接复用部分见 [upstream-synthesis.md](references/upstream-synthesis.md) 与 `THIRD_PARTY_NOTICES.md`。

本实现的脚本、模板和框架为独立重写；未直接复制没有明确许可证的上游代码或大段文档。
