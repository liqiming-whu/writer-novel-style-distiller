# v1.3.0

本版本正式发布条件式幽默感审计、第三个内置档案《龙族1—4》，并把鲁迅精选集部分样本的文笔与幽默审计保存为仓库根目录的权威回归测试案例。

## Highlights

- 幽默审计改为条件触发：只有幽默跨独立位置、跨场景稳定复现并承担叙事功能时才生成；严肃作品不因缺少幽默被扣分。
- 幽默完成度使用八项评分：机制多样性、人物绑定度、节奏与时机、声音区分度、严肃场景兼容、克制与反漫画化、情绪转化能力及综合完成度。
- 新增 `003-longzu-jiangnan`，包含《龙族1—4》的文笔与幽默审计、路明非代入机制、楚子航与绘梨衣角色魅力，以及四个卷级尾声分析。
- 四作对比报告中的《龙族》由外部暂定结论升级为正式蒸馏结果。
- 根目录新增 [`LUXUN_AUDIT_BENCHMARK.md`](LUXUN_AUDIT_BENCHMARK.md)：
  - 69个语义单位、248,469有效字符、40个分层精读单位；
  - 文笔金标准9.8／10，幽默审计必须触发，幽默完成度金标准9.9／10；
  - 强制排除萧红回忆、周作人评析、《呐喊》考点手册、编者材料及《故事新编》篇前非原创材料；
  - 只测试文笔与幽默审计，不复制鲁迅思想、人格或历史身份。
- 新增审计案例静态校验器，并纳入端到端自检和 GitHub CI。

## Benchmark boundary

鲁迅案例只是一份仓库根目录的公开脱敏金标准，不是第四个内置文风档案：

- 不进入 `profiles/`；
- 不登记到 `profiles/CATALOG.md`；
- 不由 `SKILL.md` 自动加载；
- 不包含来源原文、私有分析目录、清洗语料或连续长段引文；
- “权威”仅指本项目的回归预期答案，不表示评分是客观文学排名。

## Verification

发布前通过：

```bash
python3 scripts/validate_audit_benchmark.py
python3 scripts/validate_builtin_profiles.py
python3 scripts/self_check.py
```

并验证发布压缩包：

- 不包含 `.git`、私有语料或分析中间产物；
- 根目录存在 `SKILL.md`、`VERSION` 与 `LUXUN_AUDIT_BENCHMARK.md`；
- `VERSION` 为 `1.3.0`。

## Upgrade note

- `v1.1.0` 之后的条件式幽默审计与《龙族》档案均包含在本版本中。
- 鲁迅案例不改变内置档案加载目录；既有 `001`、`002`、`003` 调用方式保持不变。
- 版本号按发布计划直接升级为 `1.3.0`。
